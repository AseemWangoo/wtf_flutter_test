# Copy to guru_app/.env and trainer_app/.env (or use --dart-define)

# Token server (Android emulator → 10.0.2.2)
TOKEN_SERVER_URL=http://10.0.2.2:3000

# 100ms template (same as token_server)
HMS_TEMPLATE_ID=your_template_id_here
